- unless smart_listing.empty?
  .table-responsive
    %table.table.table-striped
      %thead
        %th.col-md-2= smart_listing.sortable "Título", "title"
        %th.col-md-2= smart_listing.sortable "Categoria", "cat"
        %th.col-md-2= smart_listing.sortable "Precio", "price"
        %th.col-md-2= smart_listing.sortable "Unidad", "unidad"
        %th.col-md-2= smart_listing.sortable "Ciudad", "city"
        %th.col-md-2= smart_listing.sortable "Fecha", "fecha"

      %tbody
        - smart_listing.collection.each do |ocio|
          %tr
            -# User row goes here...
            %td
              = link_to ocio do
                = truncate(ocio.title, length: 25)
                %span.badge= ocio.comments.count
            %td= ocio.cat
            %td= ocio.price
            %td= ocio.unidad
            %td= ocio.city
            %td= ocio.fecha.strftime('%d %b %Y %l:%M %p')


  -# Render nice pagination links fitted for Bootstrap 3 by default
  = smart_listing.paginate
  = smart_listing.pagination_per_page_links
- else
  %p.warning Ningún ocio!
