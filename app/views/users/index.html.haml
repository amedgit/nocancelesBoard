.col-md-8.col-md-offset-2
  .panel.panel-default
    .panel-heading
      %h1 Lista de los usuarios
    .panel-body
      - @users.each do |user|
        - if user.perfil.blank?
          = user.email
        - else
          = link_to user.email , user.perfil

        %br
